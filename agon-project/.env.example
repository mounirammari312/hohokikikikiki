# Copy this file to .env.local (for local dev with `vercel dev`)
# or set the variable in Vercel → Project → Settings → Environment Variables.

# MongoDB Atlas connection string.
# Get it from: https://cloud.mongodb.com/v2/... → Connect → Drivers
# Format: mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/lumiere
MONGODB_URI=
